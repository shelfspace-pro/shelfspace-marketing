---
description: Deep marketing spec with Pass 0 + 3 audit passes; STOPS without writing
argument-hint: <what to spec>
---

Invoke the `spec` skill via the Skill tool to handle this request. Pass the arguments below through to the skill verbatim.

$ARGUMENTS
