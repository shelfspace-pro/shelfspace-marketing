---
description: Create a new blog post end-to-end (HTML + schema + sitemap + cross-links + commit)
argument-hint: <topic / angle / type>
---

Invoke the `blog-post` skill via the Skill tool to handle this request. Pass the arguments below through to the skill verbatim.

$ARGUMENTS
