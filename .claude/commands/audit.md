---
description: Deep marketing audit of recently completed work; reports without fixing
argument-hint: <scope — e.g. "this session", "last commit", "PR #N", or omit>
---

Invoke the `audit` skill via the Skill tool to handle this request. Pass the arguments below through to the skill verbatim.

$ARGUMENTS
