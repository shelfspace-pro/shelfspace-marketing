---
description: Propose memory/skill persistence changes from session learnings; STOPS for approval
argument-hint: <scope — e.g. "all", "memory only", or omit for full session>
---

Invoke the `reflect` skill via the Skill tool to handle this request. Pass the arguments below through to the skill verbatim.

$ARGUMENTS
